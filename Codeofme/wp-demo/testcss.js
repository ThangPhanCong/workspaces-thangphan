var button = document.getElementById('bet');
button.addEventListener("click", () => { // Khi click button
  // require("./index.css")
  import('./index.css').then(()=>{ // chúng ta sẽ import xinchao.css vào
    });
});
